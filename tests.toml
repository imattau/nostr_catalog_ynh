test_format = 1.0

# The daemon's own settings are exposed through config_panel.toml rather
# than install questions; only a domain is asked at install, for the
# admins-only attestation page. Runtime and install smoke tests are kept
# in CI and should be expanded when a YunoHost test server is available.
[default]
